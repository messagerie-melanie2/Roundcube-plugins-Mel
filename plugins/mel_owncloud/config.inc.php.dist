<?php

// URLs to ownCloud instance (use same url as rc to avoid cross domains problems)
$rcmail_config['owncloud_url'] = '';
$rcmail_config['owncloud_external_url'] = '';

// DES key for password
$rcmail_config['roundcube_owncloud_des_key'] = 'my_key_is_good_need_to24';

// Filter LDAP
$rcmail_config['roundcube_owncloud_filter_ldap'] = array();
